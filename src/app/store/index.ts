import { ActionReducerMap } from "@ngrx/store";
import { ArticlesState, reducer } from "./articles.reducer";

// export interface AppState {
//     app: ArticlesState;
//   }
  
//   export const reducers: ActionReducerMap<AppState> = {
//     app: reducer,
//   };